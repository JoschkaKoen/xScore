---
description: Write clean, maintainable code and self-review every change for quality, correctness, and bugs
alwaysApply: true
---

# Clean & Maintainable Code

Self-review every change before presenting it:

1. **Correctness** — edge cases covered (null/empty, off-by-one, type mismatch)?
2. **Bugs** — typos, wrong variable, inverted condition, missing return?
3. **Simplicity** — can any block be simplified or replaced by a well-named helper?
4. **Duplication** — extract repeated logic; don't copy-paste.
5. **Naming** — names say *what*, not *how*.
6. **Dead code** — remove unused variables, imports, and commented-out blocks.
7. **Error handling** — failures are surfaced clearly; never silently swallowed.
8. **Single responsibility** — one function/class, one job. Split if it grows too large.

## Refactoring

- Keep the public interface unless explicitly asked to change it.
- Edit the smallest region possible; don't reformat unrelated lines.
- Fix pre-existing bugs found nearby and call them out.
