# Secrets / local env
.env
.env.*

# Local / generated
.venv/
paddle_env/
__pycache__/
*.py[cod]
.DS_Store

# PDFs (exam scans, reports) — never commit; may contain student data
*.pdf

# Source scan folder and processed output trees (never commit or push — local only)
Space Physics Unit Test/
output/

# Ground-truth / evaluation labels (local only; do not publish)
Ground Truth*

# Local agent / benchmark logs
improvement_log.jsonl
logs/
